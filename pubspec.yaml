name: smith_cli
description: A code generator to create datasource, repository, interface, service, etc
version: 0.0.1
# repository: https://github.com/my_org/my_repo

environment:
  sdk: '>=2.19.2 <3.0.0'

# dependencies:
#   path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0

executables:
  smith: smith_cli

platforms:
  windows:
  linux:
  macos:
